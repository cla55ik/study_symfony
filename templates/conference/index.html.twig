{% extends 'base.html.twig' %}

{% block title %}Conference guest block{% endblock %}

{% block body %}

<div class="example-wrapper">
    <h2>Give your feedback!</h2>

    {% for conference in conferences %}
    <h4>{{ conference }}</h4>
        <p>
            <a href="{{ path('conference',{id:conference.id}) }}">view</a>
        </p>
    {% endfor %}
</div>
{% endblock %}
